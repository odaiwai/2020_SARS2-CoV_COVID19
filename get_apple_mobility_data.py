#!/usr/bin/env python3
""" Download and plot the Apple Mobility Data"
"""
import os
import json
import requests

def main():
    # get the index json
    idx_uri = r'https://covid19-static.cdn-apple.com/covid19-mobility-data/current/v1/index.json'
    index_json = request(idx_uri)
    for 
    # Walk it and get the mobility files
    # Download them and build some graphs
if __name__ == '__main__':
    locale = en_us
    main()
